# Resume Pilot v1.0 客户交付与验收指南

## 支持环境

- Microsoft Edge 或 Google Chrome 的当前稳定版本。
- 通过“开发人员模式 → 加载解压缩的扩展”安装。
- 招聘官网必须可在浏览器中正常访问；登录、短信验证和验证码由官网决定。

## 安装

1. 使用 7-Zip 将发行包解压到独立文件夹。压缩包内只有一个顶层目录，避免文件散落。
2. Edge 打开 `edge://extensions`，Chrome 打开 `chrome://extensions`。
3. 开启“开发人员模式”，选择“加载解压缩的扩展”，选中包含 `manifest.json` 的目录。
4. 更新版本时，用新目录替换旧目录，然后在扩展管理页点击“重新加载”。个人资料保存在浏览器扩展存储中；卸载扩展可能清除这些数据。

## 首次验收

1. 导入一份测试简历，确认姓名、联系方式、学校、技能和目标岗位识别正确。
2. 目标城市选择一个或多个城市，或者选择“不限”。
3. 点击“发现企业并核验岗位入口”。企业候选不等于存在合适岗位。
4. 对一个候选点击“打开官网并核验真实岗位”，确认扩展显示官网真实岗位数和匹配数。
5. 运行模式选择“试运行”，本次最多处理岗位设为 `1`。确认它能进入详情、复核地点并填写表单，但不会点击最终提交。
6. 验收通过后，再使用“提交前由我确认”；最后才启用“全自动最终提交”并勾选授权。

## 状态含义

- 企业候选：只确认了官方招聘入口。
- 岗位线索：搜索结果可能指向具体岗位，但尚未在官网页面核验。
- 官网真实岗位：已从官网页面读取岗位。
- 已验证匹配：岗位通过当前城市、类型、技能和排除条件。
- 已验证无匹配：官网有岗位，但当前没有符合条件的岗位。
- 流程待兼容：官网入口、岗位卡或申请流程无法可靠确认，扩展会暂停而不是冒险点击。

## 运行模式

- 试运行：完成岗位、地点和表单核验，填写已知字段，不提交。
- 提交前由我确认：填写完成后暂停；用户检查并手动提交，再点击“处理后继续”核验结果。
- 全自动最终提交：仅在用户勾选授权、资料完整、地点符合且没有验证码时点击明确的最终提交按钮。

## 安全边界

- 不绕过验证码、短信验证、登录安全检查或招聘网站限制。
- 不猜测未知必填项；遇到新问题会暂停询问并保存答案。
- 地点明确不符、岗位已结束、经验要求过高或招聘类型明确不符时不会投递。
- 同一岗位使用公司、岗位名称、ATS 域名和岗位 ID 防重复。
- 官网结构发生变化时可能进入“流程待兼容”；可导出脱敏诊断文件用于排查。

## 客户支持信息

出现问题时请提供：扩展版本、公司名称、官网页面地址、页面截图和“导出脱敏诊断”文件。诊断文件不包含姓名、手机号、邮箱、简历正文、附件或已保存答案。
