# Resume Pilot v2 混合架构

## 设计目标

v2 不用桌面软件替代浏览器，而是把适合长期运行和批量计算的能力放进桌面端，把必须依赖真实网页、登录状态和用户接管的能力留在扩展中。

## 模块边界

### 桌面控制中心

- 本地岗位元数据仓库与失效时间。
- 简历条件对应的岗位排序、硬条件过滤和队列生成。
- 每家企业岗位上限、全局去重和投递状态看板。
- 每个招聘域名单独限速，429 自动退避。
- 后续接入经过授权的官方 API、ATS、RSS 或公开岗位数据源。

### 浏览器执行器

- 使用用户当前 Edge/Chrome 登录状态。
- 识别企业官网、ATS、招聘入口、岗位列表和详情。
- 官网关键词筛选、分页、城市复核、站内简历和表单填写。
- 验证码、短信验证、账号选择和未知必填项由用户处理。
- 只有明确授权的全自动模式才能点击最终提交。

### 本地通信

- 使用 Chrome Native Messaging 的长度前缀 JSON 协议。
- 协议主机名：`com.resume_pilot.desktop`。
- 扩展仅同步岗位元数据和求职偏好，不同步身份资料或简历附件。
- 安装版把 Native Messaging 作为必需权限；只有固定扩展 ID 能访问当前用户注册的本地主机。

## 限速策略

- 同一招聘域名最多一个进行中的任务。
- 正常完成后保留最小访问间隔。
- HTTP 429 按失败次数指数退避，并尊重更长的 `Retry-After`。
- 不通过切换 IP、绕过验证码或模拟攻击性并发规避官网限制。

## 后续里程碑

1. 把本地主机打包为独立签名可执行文件。
2. 制作仅写入当前用户范围的安装程序，并在执行注册表安装前获得用户授权。
3. 插件从桌面队列领取岗位并回传试运行、待确认、已提交和失败状态。
4. 增加公开岗位数据源适配器和定时更新计划。
5. 完成 Windows 安装包、升级、卸载和数据导出验收。
